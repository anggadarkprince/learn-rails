<% content_for :title do %> - Edit <%= @article.title %>
<% end %>

<%= render 'author/header' %>

<div class="container">
  <div class="row">
    <div class="col-lg-8 ml-lg-auto mr-lg-auto">
      <div class="mb-5">
        <div class="mb-4">
          <h4>
            <a href="<%= articles_path %>">
              <i class="icon-arrow-left-circle mr-1 align-middle"></i>
            </a>
            Edit Post
          </h4>
          <p class="text-muted">Update old article</p>
        </div>
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>
